"""
Main entry point for running the RCM Capacity Planning Model.

This is the only entry point for running the RCM model. The implementation
is in the rcm package, but should be run through this file.

Usage:
    python3 main.py
"""

from rcm.main import main

if __name__ == "__main__":
    main() 